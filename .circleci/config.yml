version: 2.1
orbs:
  docker: circleci/docker@2.6.0
jobs:
  build-and-push:
    executor: docker/docker
    steps:
      - setup_remote_docker
      - checkout
      - run: 'docker login -u $DOCKERHUB_LOGIN -p $DOCKERHUB_PASSWORD'
      - run: docker build -t api-feed ./udagram-api-feed
      - run: docker push api-feed
workflows:
  commit:
    jobs:
      - build-and-push
